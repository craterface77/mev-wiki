## How to build simulated blockchain realities (and rule that world)

**_[Abstract]_**

**GM.**

You wake up in the morning feeling very confident about your progress yesterday. Now you’re about to begin working on your Solidity smart contract. _“Oh, this shouldn’t be so difficult; it’s just Solidity after all.”_

And indeed, it’s **_just_** Solidity after all.

However, you’re now on your first function call — which only took 5 minutes of your time to write. “_Oh, I’m fast”_, you think. It’s an all too familiar “**swap**” call to a Uniswap V2 pair contract that you’d like to fire up using your MEV bot. Just one more step away and you know you’re done.

But life isn’t easy. You deploy your contract to the Testnet or the Mainnet using Remix / Foundry / Hardhat / Brownie (you name it). And as soon as you run “swap”,

> EVM REVERT 0x\b2…

**GN.**

**Repeat…**

So that’s what smart contract development looks like from behind the scenes. Setting aside the glory, most of our time is dedicated to testing and simulating our contract calls, making sure that state changes occur as expected.

Even with the amazing tooling available to assist us now, it can still become quite overwhelming.

The challenge intensifies when we attempt to override storage values and create an environment tailored specifically to our needs. For instance, when:

- **we want to simulate the price impact of swapping 1,000,000 WETH on a Uniswap pool _(to see what it feels like to become a whale_** 🐳**_)_ but don’t have that much WETH,**
- **we want to determine if we’re dealing with a honeypot token without actually buying them,**
- **we want to simulate our arbitrage path to see if it’ll be profitable**

we have to mess around with the default storage values and inject values that we’d like to test out.

All of this can actually be accomplished in the old-school way, without resorting to fancy techniques, by delving into open-sourced GitHub code and documentation to check if the token of interest is a honeypot.

Moreover, you can study DEX mathematics yourself, which will compel you to explore various protocols like Uniswap, Curve, Balancer, and the like.

**However, it quickly becomes apparent that this approach is not scalable at all.**

💥 So all the cool kids are using something called **EVM simulation** nowadays. And we’re about to explore this exciting territory using a fantastic Rust library known as **REVM**:

[

## GitHub - bluealloy/revm: Rust Ethereum virtual machine (revm) Is EVM written in rust that is…

### Rust Ethereum virtual machine (revm) Is EVM written in rust that is focused on speed and simplicity - GitHub …

github.com



](https://github.com/bluealloy/revm?source=post_page-----e01b5b0421e4---------------------------------------)

I’ve actually touched on this subject in my previous blog post:

[

## Using REVM to build a sandwich bot

### In the last post, How I spend my days Mempool watching (Part 1): Transaction Prediction through EVM Tracing, we…

medium.com



](https://medium.com/@solidquant/why-i-think-everyone-should-use-revm-rust-evm-art-of-mempool-watching-part-2-c67308d5cf03?source=post_page-----e01b5b0421e4---------------------------------------)

where I promised to build a sandwich bot using REVM, but didn’t (sorry about that 🙏 We’ll get there soon!)… While my previous blog post could certainly provide some background, it’s not a requirement. Anyone who hasn’t read it yet can dive right into this post and follow along seamlessly.

In this blog post:

1. We’ll dive deeper and see the inner workings of **REVM**, how it works and what we can do with the library. We’ll learn how to: **(a)** **use revm to perform EVM tracing, (b) find storage slot values of contract variables, and (c) get around the proxy pattern to get the implementation contract’s bytecode, and much more.**
2. We’ll also see that REVM alone can sometimes be “too low-level”, so we’ll use **foundry-evm (**[https://github.com/foundry-rs/foundry/tree/master/crates/evm](https://github.com/foundry-rs/foundry/tree/master/crates/evm)) to work with a higher level interface. You’ll be surprised to see that **foundry-evm** can take care of everything we did above with a single line of code.
3. Lastly, we’ll also use the **eth_call** method to show you how all this can be done without any fancy techniques if we’re simply dealing with a single transaction simulation.

We will try to simulate a simple **“swap”** function in Uniswap V2 to illustrate how this can be done.

Let’s have fun! 🎉

⭐️ _Python and JavaScript users can also benefit significantly from these features._ And Pythonistas actually have the option to use the **pyrevm** library, which is a native wrapper around **revm** utilizing **PyO3**. It’s extremely fast, according to the benchmarks done in [https://github.com/ziyadedher/evm-bench](https://github.com/ziyadedher/evm-bench). _(What happened to JS though?)_

Press enter or click to view image in full size

![](https://miro.medium.com/v2/resize:fit:1400/1*WJPGZPtlFlWLbB6HLFp0aA.png)

However, it’s worth noting that the library was last updated nine months ago and may not offer full **revm** functionalities. So initially, I intended to cover **pyrevm** in this blog post but had to postpone it.

**I’ll make sure to deal with _pyrevm_ content once I’ve dug deeper.**

[

## GitHub - gakonst/pyrevm: Python wrapper around https://github.com/bluealloy/revm/ using PyO3

### Python wrapper around https://github.com/bluealloy/revm/ using PyO3 - GitHub - gakonst/pyrevm: Python wrapper around…

github.com



](https://github.com/gakonst/pyrevm?source=post_page-----e01b5b0421e4---------------------------------------)

## **What is EVM simulation, and why so serious?**

EVM simulation isn’t a novel concept; it’s something we’re all familiar with and engage in regularly.

There are two primary methods for simulating transactions:

1. **Offline simulation:** In this approach, you perform all calculations locally without relying on network connections (RPC node connection). This method is typically much faster since most bottlenecks arise from network connections.
2. **Online simulation:** Transactions are executed using an EVM machine, often through a blockchain node. This method is referred to as “online” because it involves interacting with the blockchain network.

However, there’s another method to execute transactions without sending them directly to the node. You can isolate the EVM machine component from the node and run it locally, significantly accelerating simulations.

By doing this, you reduce the number of RPC calls to the node, minimizing network latency. However, we do still need access to the node to retrieve storage values, so **EVM simulation falls within the “Online simulation” category.**

## REVM tutorial begins here

Getting our hands dirty is the best way to understand the technology. If everything I said above was too vague, start typing along with me, and things will get crystal clear.

Let’s start a fresh new Rust project. I’ll be explaining everything line by line, so people who aren’t too familiar with using Rust can still follow along.

**All the code used in this blog post is available on my Github repo:**

[

## GitHub - solidquant/revm-is-all-you-need: EVM simulation tutorials and samples (revm / foundry-evm…

### EVM simulation tutorials and samples (revm / foundry-evm / eth_call) - GitHub - solidquant/revm-is-all-you-need: EVM…

github.com



](https://github.com/solidquant/revm-is-all-you-need?source=post_page-----e01b5b0421e4---------------------------------------)

🚨**🔥WARNING BEFORE WE BEGIN🔥**🚨

_This blog post will go down the rabbit hole_ 🕳🐇 _— just like I did researching these topics. So naturally, it’ll get complicated pretty fast, but it’ll also be very rewarding in the end. So take your time. Break things apart, and try to understand how each of the sample codes here work. Maybe trying to squeeze everything into this single article was too far-fetched haha._

_Oh, but don’t worry. It’s not so bad once you take the time 🙏_

**⚡️⚡️ For readers that get stuck anywhere, feel free to join the Discord server, where more than 500 members are already discussing MEV related topics on a daily basis. I’m thinking about creating a separate channel for EVM simulation and share some more ideas there as well:**

[

## Join the Solid Quant Discord Server!

### Quant hobbyists doing quant stuff - mainly MEV related at the moment. | 506 members

discord.com







](https://discord.com/invite/e6KpjTQP98?source=post_page-----e01b5b0421e4---------------------------------------)

### ✅ Init

Run the following command:

▶️ cargo new revm-is-all-you-need

This will create an empty Rust project directory for you. Next, copy and paste the below dependencies into your **Cargo.toml** file **(Cargo.toml)**:

[package]  
name = "revm-is-all-you-need"  
version = "0.1.0"  
edition = "2021"  
  
[dependencies]  
dotenv = "0.15.0"  
futures = "0.3.5"  
anyhow = "1.0.70"  
tokio = { version = "1.29.0", features = ["full"] }  
tokio-stream = { version = "0.1", features = ['sync'] }  
  
ethers-core = "2.0"  
ethers-providers = "2.0"  
ethers-contract = "2.0"  
ethers = { version = "2.0", features = ["abigen", "ws"]}  
  
foundry-evm = { git = "https://github.com/solidquant/foundry.git", branch = "version-fix" }  
anvil = { git = "https://github.com/solidquant/foundry.git", branch = "version-fix" }  
  
revm = { version = "3.3.0", features = ["ethersdb"] }  
  
eth-encode-packed = "0.1.0"  
  
colored = "2.0.0"  
log = "0.4.17"  
fern = {version = "0.6.2", features = ["colored"]}  
chrono = "0.4.23"

Let’s take care of all the dependencies right from the get-go. It’s a hassle to plug it in one at a time.

🛑 **One thing to note here is that:**

- We’re using a forked version of the Foundry project from a previous commit ([https://github.com/solidquant/foundry](https://github.com/solidquant/foundry)). **This is to ensure that version conflicts don’t happen**. Ethers-rs, Foundry, and REVM keep their projects up to date all the time, which is amazing. However, the downside is that these frequent updates can occasionally lead to version conflicts. _Use the forked version for now, and once you get the hang of it, update your codebase to newer versions._
- **revm** has been updated to version 3.5 as of now, and even the forked version of our Foundry is breaking. For this I’ve created a lite version of Foundry, which you can check out here:

[

## GitHub - solidquant/foundry-evm-mini: Foundry EVM Standalone

### Foundry EVM Standalone. Contribute to solidquant/foundry-evm-mini development by creating an account on GitHub.

github.com



](https://github.com/solidquant/foundry-evm-mini?source=post_page-----e01b5b0421e4---------------------------------------)

_You can read the_ **_README.md_** _file for how foundry-evm-mini can be used, and view the example in_ **_src/main.rs_**_._

==Now, comes the best part of any tutorial. Running the main function and seeing if the setup works==.

Type this into your **src/main.rs**:

use anyhow::Result;  
  
#[tokio::main]  
async fn main() -> Result<()> {  
    println!("Hello, world!");  
    Ok(())  
}

We’ll be using **“anyhow”** to simplify error handling. The project should build and spit out on running **cargo run**:

Hello, world!

_If only all tutorial steps were this easy._ But things are about to get a whole lot more hectic, so hold on tight. 🎢

### ✅ Creating the EVM instance

Let’s create another Rust file within our **src** directory (**src/revm_examples.rs**):

use revm::{  
    db::{CacheDB, EmptyDB, InMemoryDB},  
    EVM,  
};  
  
pub fn create_evm_instance() -> EVM<InMemoryDB> {  
    let db = CacheDB::new(EmptyDB::default());  
    let mut evm = EVM::new();  
    evm.database(db);  
    evm  
}

We create a simple function that returns an instance of the EVM using **InMemoryDB** which is equivalent to **CacheDB<EmptyDB>>**. This will create a clean slate EVM environment that doesn’t have any storage values in it. So no accounts or contracts exist yet. We’ll have to deal with everything on our own.

Next, we create another short function to setup some basic EVM configurations (**src/revm_examples.rs**):

// imports  
  
// create_evm_instance  
  
// add this 👇  
pub fn evm_env_setup(evm: &mut EVM<InMemoryDB>) {  
    // overriding some default env values to make it more efficient for testing  
    evm.env.cfg.limit_contract_code_size = Some(0x100000);  
    evm.env.cfg.disable_block_gas_limit = true;  
    evm.env.cfg.disable_base_fee = true;  
}

There’re a couple of configurations we can customize through the **cfg** field:

Press enter or click to view image in full size

![](https://miro.medium.com/v2/resize:fit:1400/1*Gxaq6a-_FenSAbPpOaxI8g.png)

As you can see, we can override the default **contract size limit** which is **0x6000** _(=24,576 bytes)_ by default, and balance checks, along with block gas limits, and base fee checks. In our example, I’ve set the contract size limit to **0x100000** _(=104,8576 bytes)_, to relax the constraints a bit for ease of testing.

### ✅ Retrieving ERC-20 token balance

We’ll try calling the ERC-20 function **“balanceOf”** to check how much of the token balance our simulated user holds (**src/revm_examples.rs**):

// add all the necessary imports 👇  
use anyhow::{anyhow, Result};  
use bytes::Bytes;  
use ethers::{  
    abi::{self, parse_abi},  
    prelude::*,  
    providers::Middleware,  
    types::{  
        transaction::eip2930::AccessList, BlockId, BlockNumber, Eip1559TransactionRequest,  
        NameOrAddress, H160, U256,  
    },  
};  
use log::info;  
use revm::{  
    db::{CacheDB, EmptyDB, EthersDB, InMemoryDB},  
    primitives::Bytecode,  
    primitives::{  
        keccak256, AccountInfo, ExecutionResult, Log, Output, TransactTo, TxEnv, B160,  
        U256 as rU256,  
    },  
    Database, EVM,  
};  
use std::{str::FromStr, sync::Arc};  
  
// create_evm_instance  
  
// evm_env_setup  
  
// add this 👇  
#[derive(Debug, Clone)]  
pub struct TxResult {  
    pub output: Bytes,  
    pub logs: Option<Vec<Log>>,  
    pub gas_used: u64,  
    pub gas_refunded: u64,  
}  
  
pub fn get_token_balance(evm: &mut EVM<InMemoryDB>, token: H160, account: H160) -> Result<U256> {  
    let erc20_abi = BaseContract::from(parse_abi(&[  
        "function balanceOf(address) external view returns (uint256)",  
    ])?);  
    let calldata = erc20_abi.encode("balanceOf", account)?;  
  
    evm.env.tx.caller = account.into();  
    evm.env.tx.transact_to = TransactTo::Call(token.into());  
    evm.env.tx.data = calldata.0;  
  
    // This will fail, because the token contract has not been deployed yet  
    let result = match evm.transact_ref() {  
        Ok(result) => result,  
        Err(e) => return Err(anyhow!("EVM call failed: {e:?}")),  
    };  
    let tx_result = match result.result {  
        ExecutionResult::Success {  
            gas_used,  
            gas_refunded,  
            output,  
            logs,  
            ..  
        } => match output {  
            Output::Call(o) => TxResult {  
                output: o,  
                logs: Some(logs),  
                gas_used,  
                gas_refunded,  
            },  
            Output::Create(o, _) => TxResult {  
                output: o,  
                logs: Some(logs),  
                gas_used,  
                gas_refunded,  
            },  
        },  
        ExecutionResult::Revert { gas_used, output } => {  
            return Err(anyhow!(  
                "EVM REVERT: {:?} / Gas used: {:?}",  
                output,  
                gas_used  
            ))  
        }  
        ExecutionResult::Halt {  
            reason, gas_used, ..  
        } => return Err(anyhow!("EVM HALT: {:?} / Gas used: {:?}", reason, gas_used)),  
    };  
    let decoded_output = erc20_abi.decode_output("balanceOf", tx_result.output)?;  
    Ok(decoded_output)  
}

We’ll go over everything line by line.

First, we create the **BaseContract** instance from a simple ERC-20 ABI that contains a single function definition:

let erc20_abi = BaseContract::from(parse_abi(&[  
    "function balanceOf(address) external view returns (uint256)",  
])?);  
let calldata = erc20_abi.encode("balanceOf", account)?;

With this, we encode the function call to **“balanceOf”**, which will result in the below, if we try printing out **“calldata”**:

Bytes(0x70a08231000000000000000000000000e2b5a9c1e325511a227ef527af38c3a7b65afa1d)

We, then, have to setup the transaction variables before we can send the call:

evm.env.tx.caller = account.into();  
evm.env.tx.transact_to = TransactTo::Call(token.into());  
evm.env.tx.data = calldata.0;

Nothing too complicated, we’re only setting values for three variables:

- **caller:** who is calling the function (from)
- **transact_to:** what are we calling (to)
- **data:** the input data of our transaction (input)

After this, we run our transaction as below:

let result = match evm.transact_ref() {  
    Ok(result) => result,  
    Err(e) => return Err(anyhow!("EVM call failed: {e:?}")),  
};

You can see that we’re calling the **“transact_ref”** function on our EVM to run our transaction. As a matter of fact, there’re a total of six ways to run your transactions in **revm**, which are:

- **_transact:_** _execute tx without writing to DB, returns the states_
- **_inspect:_** _execute tx with inspector, and without writing to DB, returns the states_
- **transact_commit:** call **“transact”** and commit the state changes to DB
- **inspect_commit:** call **“inspect”** with inspector, and commit the state changes to DB
- **transact_ref:** call **“transact”** and do not commit changes to DB
- **inspect_ref:** call **“inspect”** with inspector, and do not commit changes to DB

So, basically, we call **transact_ref/inspect_ref** to run transactions without any DB changes.

Upon successfully running the transaction, we’ll receive a **ResultAndState** struct that looks something like:

Press enter or click to view image in full size

![](https://miro.medium.com/v2/resize:fit:1400/1*tTGv_qnyJVEb0ij2-czF3w.png)

_(Take a quick snapshot of how this data looks like. We’ll only be using the_ **_result_** _field for now, but there is a use for the_ **_state_** _field as well, which we will soon get into. Nothing will go to waste.)_

which we’ll match the **result** field using the following code:

let tx_result = match result.result {  
    ExecutionResult::Success {  
        gas_used,  
        gas_refunded,  
        output,  
        logs,  
        ..  
    } => match output {  
        Output::Call(o) => TxResult {  
            output: o,  
            logs: Some(logs),  
            gas_used,  
            gas_refunded,  
        },  
        Output::Create(o, _) => TxResult {  
            output: o,  
            logs: Some(logs),  
            gas_used,  
            gas_refunded,  
        },  
    },  
    ExecutionResult::Revert { gas_used, output } => {  
        return Err(anyhow!(  
            "EVM REVERT: {:?} / Gas used: {:?}",  
            output,  
            gas_used  
        ))  
    }  
    ExecutionResult::Halt {  
        reason, gas_used, ..  
    } => return Err(anyhow!("EVM HALT: {:?} / Gas used: {:?}", reason, gas_used)),  
};

Since **ExecutionResult** can come in three flavors _(Success / Revert / Halt)_, we handle all of them and extract the variables that we’re interested in. Try skimming through the **ExecutionResult** definition:

Press enter or click to view image in full size

![](https://miro.medium.com/v2/resize:fit:1400/1*h4SsqEQYWMzQyIdj1dvNzA.png)

We extract out _gas_used, gas_refunded, logs, and output_ and create a separate **TxResult** struct variable that is defined as:

pub struct TxResult {  
    pub output: Bytes,  
    pub logs: Option<Vec<Log>>,  
    pub gas_used: u64,  
    pub gas_refunded: u64,  
}

Lastly, we decode the Bytes output using the **erc20_abi** BaseContract instance:

let decoded_output = erc20_abi.decode_output("balanceOf", tx_result.output)?;  
Ok(decoded_output)

👉 We’ll try running this code now. _I want to tell you beforehand though that this will fail, so don’t panic!_ I’ll tell you why it will fail.

Create a new file in your src directory **(src/lib.rs)**:

pub mod revm_examples;  
pub mod constants;  
pub mod utils;

Create another file in you src directory **(src/constants.rs)**:

use ethers::{  
    prelude::Lazy,  
    types::{Address, Bytes},  
};  
use std::str::FromStr;  
  
pub fn get_env(key: &str) -> String {  
    std::env::var(key).unwrap()  
}  
  
#[derive(Debug, Clone)]  
pub struct Env {  
    pub https_url: String,  
    pub wss_url: String,  
}  
  
impl Env {  
    pub fn new() -> Self {  
        Env {  
            https_url: get_env("HTTPS_URL"),  
            wss_url: get_env("WSS_URL"),  
        }  
    }  
}  
  
pub static ZERO_ADDRESS: Lazy<Address> =  
    Lazy::new(|| Address::from_str("0x0000000000000000000000000000000000000000").unwrap());  
  
pub static SIMULATOR_CODE: Lazy<Bytes> = Lazy::new(|| {  
    ""  
        .parse()  
        .unwrap()  
});

Don’t mind the **SIMULATOR_CODE** variable for now, we’ll be using that in a moment.

Another **(src/utils.rs)**, which is to help out with our logging:

use anyhow::{self, Result};  
use fern::colors::{Color, ColoredLevelConfig};  
use log::LevelFilter;  
  
pub fn setup_logger() -> Result<()> {  
    let colors = ColoredLevelConfig {  
        trace: Color::Cyan,  
        debug: Color::Magenta,  
        info: Color::Green,  
        warn: Color::Red,  
        error: Color::BrightRed,  
        ..ColoredLevelConfig::new()  
    };  
  
    fern::Dispatch::new()  
        .format(move |out, message, record| {  
            out.finish(format_args!(  
                "{}[{}] {}",  
                chrono::Local::now().format("[%H:%M:%S]"),  
                colors.color(record.level()),  
                message  
            ))  
        })  
        .chain(std::io::stdout())  
        .level(log::LevelFilter::Error)  
        .level_for("revm_is_all_you_need", LevelFilter::Info)  
        .apply()?;  
  
    Ok(())  
}

Then go over to **(src/main.rs)**:

use anyhow::Result;  
use ethers::{  
    providers::{Middleware, Provider, Ws},  
    types::{BlockNumber, H160},  
};  
use log::info;  
use std::{str::FromStr, sync::Arc};  
  
use revm_is_all_you_need::constants::Env;  
use revm_is_all_you_need::revm_examples::{  
    create_evm_instance, evm_env_setup, get_token_balance  
};  
use revm_is_all_you_need::utils::setup_logger;  
  
#[tokio::main]  
async fn main() -> Result<()> {  
    dotenv::dotenv().ok();  
    setup_logger()?;  
  
    let mut evm = create_evm_instance();  
    evm_env_setup(&mut evm);  
  
    let user = H160::from_str("0xE2b5A9c1e325511a227EF527af38c3A7B65AFA1d").unwrap();  
  
    let weth = H160::from_str("0xC02aaA39b223FE8D0A0e5C4F27eAD9083C756Cc2").unwrap();  
    let usdt = H160::from_str("0xdAC17F958D2ee523a2206206994597C13D831ec7").unwrap();  
    let usdc = H160::from_str("0xA0b86991c6218b36c1d19D4a2e9Eb0cE3606eB48").unwrap();  
    let dai = H160::from_str("0x6B175474E89094C44Da98b954EedeAC495271d0F").unwrap();  
  
    let weth_balance = get_token_balance(&mut evm, weth, user);  
    info!("WETH balance: {:?}", weth_balance);  
  
    Ok(())  
}

We’ll be callling our **“get_token_balance”** function with **WETH**:

let weth_balance = get_token_balance(&mut evm, weth, user);

Now, run:

▶️ cargo run

Anddd we get this error:

Press enter or click to view image in full size

![](https://miro.medium.com/v2/resize:fit:1400/1*khqdsqRmUPuim-Ue2XRnhQ.png)

[09:19:27][INFO] WETH balance: Err(Invalid name: please ensure the contract and method you're calling exist! failed to decode empty bytes. if you're using jsonrpc this is likely due to jsonrpc returning `0x` in case contract or method don't exist)

The error message kindly tells us that we don’t have the contract and method we’re calling. But, that’s odd, because we are calling the WETH contract, which should be there.

🔴 D**o you remember when I said that our EVM instance with the backend DB running EmptyDB will be a clean slate?** This is what I meant. All the familiar contracts won’t exist in our blockchain world yet. So we have to create them one by one before we can start interacting with it.

We’ll see how we can do that in the next section.

### ✅ Building our world from the ground up

Now comes the fun part — which can also be simultaneously considered our biggest nightmare when dealing with **revm**.

Before we can start, let’s picture what we’re trying to do here:

Press enter or click to view image in full size

![](https://miro.medium.com/v2/resize:fit:1400/1*1UswXj4MuluGLYnlz4OmZg.png)

Pokemon dolls on sale from Tmon (A Korean social commerce)

Alright, so we all know that Ditto from Pokémon has the ability to transform into other Pokémon, right? But there’s just something a bit off about the way its transformed version looks.

That’s the analogy we’re drawing with our simulated environments. In the end, it may not be entirely complete, but it will still function as intended, allowing us to produce simulation results.

**Let’s take this party up a notch!**

We’re finally ready to put our **“swap”** function from Uniswap V2 to the test. To achieve this, we’ll create a straightforward smart contract to handle the task, and we’ll do so by running:

▶️ forge init contracts --no-commit

Running this will create a Foundry project template within the **contracts** directory. Create a new file within **contracts/src** directory **(contracts/src/Simulator.sol)**:

// SPDX-License-Identifier: MIT  
pragma solidity ^0.8.0;  
  
import "./interfaces/IUniswapV2Pair.sol";  
import "./interfaces/IUniswapV2Router02.sol";  
import "./interfaces/IUniswapV3Pool.sol";  
import "./interfaces/IERC20.sol";  
  
import "./utils/SafeERC20.sol";  
  
contract Simulator {  
    using SafeERC20 for IERC20;  
  
    function v2SimulateSwap(        uint256 amountIn,  
        address targetPair,  
        address inputToken,  
        address outputToken    ) external returns (uint256 amountOut, uint256 realAfterBalance) {  
        // 1. Check if you can transfer the token  
        // Some honeypot tokens won't allow you to transfer tokens  
        IERC20(inputToken).safeTransfer(targetPair, amountIn);  
  
        uint256 reserveIn;  
        uint256 reserveOut;  
  
        {  
            (uint256 reserve0, uint256 reserve1, ) = IUniswapV2Pair(targetPair)  
                .getReserves();  
  
            if (inputToken < outputToken) {  
                reserveIn = reserve0;  
                reserveOut = reserve1;  
            } else {  
                reserveIn = reserve1;  
                reserveOut = reserve0;  
            }  
        }  
  
        // 2. Calculate the amount out you are supposed to get if the token isn't taxed  
        uint256 actualAmountIn = IERC20(inputToken).balanceOf(targetPair) -  
            reserveIn;  
        amountOut = this.getAmountOut(actualAmountIn, reserveIn, reserveOut);  
  
        // If the token is taxed, you won't receive amountOut back, and the swap will revert  
        uint256 outBalanceBefore = IERC20(outputToken).balanceOf(address(this));  
  
        (uint256 amount0Out, uint256 amount1Out) = inputToken < outputToken  
            ? (uint256(0), amountOut)  
            : (amountOut, uint256(0));  
        IUniswapV2Pair(targetPair).swap(  
            amount0Out,  
            amount1Out,  
            address(this),  
            new bytes(0)  
        );  
  
        // 3. Check the real balance of outputToken after the swap  
        realAfterBalance =  
            IERC20(outputToken).balanceOf(address(this)) -  
            outBalanceBefore;  
    }  
  
    function getAmountOut(        uint256 amountIn,  
        uint256 reserveIn,  
        uint256 reserveOut    ) external pure returns (uint256 amountOut) {  
        require(amountIn > 0, "UniswapV2Library: INSUFFICIENT_INPUT_AMOUNT");  
        require(  
            reserveIn > 0 && reserveOut > 0,  
            "UniswapV2Library: INSUFFICIENT_LIQUIDITY"  
        );  
        uint256 amountInWithFee = amountIn * 997;  
        uint256 numerator = amountInWithFee * reserveOut;  
        uint256 denominator = reserveIn * 1000 + amountInWithFee;  
        amountOut = numerator / denominator;  
    }  
}

🔵 It’s important to use the **SafeERC20** contract interface, because not all ERC-20 tokens share the same interface, and SafeERC20 will help us address this issue.

❗️ **_USDT token is a well-known ERC-20 token that exhibits this behavior._** Tether doesn’t return a boolean value for its **“transfer”** function, unlike standard ERC-20 tokens.

Apart from this, the Simulator contract is relatively straightforward.

To execute the **“v2SimulateSwap”** function within our **Simulator** contract, we must transfer a certain amount of **inputToken** (held by the Simulator contract) to the **targetPair** contract before we can invoke the **“swap”** function.

This implies that we need to possess a specific quantity of **inputToken** before conducting this simulation. This can be achieved by overriding storage values using **revm**.

### 📍 Overriding “balances” mappings in ERC-20 tokens

We know a little bit about storage slots. We understand that by examining how variables are organized within a smart contract, we can determine the storage slots for those variables.

One widely recognized example is the **“balances”** mapping in the WETH token, which is known to use storage slot 3 to store user balances. This can be conveniently identified using tools such as **Slither**:

[

## GitHub - crytic/slither: Static Analyzer for Solidity

### Static Analyzer for Solidity. Contribute to crytic/slither development by creating an account on GitHub.

github.com



](https://github.com/crytic/slither/tree/master?source=post_page-----e01b5b0421e4---------------------------------------)

👉 We require this information because to override state values using **revm** or any other simulation engines, we must inject values directly into the storage slot. Therefore, if we are aware of the storage slot values for the **“balances”** mappings of ERC-20 tokens, we can simulate transactions that necessitate a balance of those tokens, even if we actually possess none.

We’ll quickly go over a demo of how to use **Slither** to figure out the storage layout of smart contracts that are verified on Etherscan. This is how I initially figured out the storage slot values for various smart contracts. If you have Python installed on your machine, try running:

▶️ pip install slither-analyzer

Next, get the contract address for WETH, which is:

> **0xC02aaA39b223FE8D0A0e5C4F27eAD9083C756Cc2**

and run:

▶️ slither-read-storage 0xC02aaA39b223FE8D0A0e5C4F27eAD9083C756Cc2 --json weth_storage.json

this will output the storage layout in the _weth_storage.json_ file:

{  
    "name": {  
        "name": "name",  
        "type_string": "string",  
        "slot": 0,  
        "size": 256,  
        "offset": 0,  
        "value": null,  
        "elems": {}  
    },  
    "symbol": {  
        "name": "symbol",  
        "type_string": "string",  
        "slot": 1,  
        "size": 256,  
        "offset": 0,  
        "value": null,  
        "elems": {}  
    },  
    "decimals": {  
        "name": "decimals",  
        "type_string": "uint8",  
        "slot": 2,  
        "size": 8,  
        "offset": 0,  
        "value": null,  
        "elems": {}  
    },  
    "balanceOf": {  
        "name": "balanceOf",  
        "type_string": "mapping(address => uint256)",  
        "slot": 3,  // 👈 right here!!  
        "size": 256,  
        "offset": 0,  
        "value": null,  
        "elems": {}  
    },  
    "allowance": {  
        "name": "allowance",  
        "type_string": "mapping(address => mapping(address => uint256))",  
        "slot": 4,  
        "size": 256,  
        "offset": 0,  
        "value": null,  
        "elems": {}  
    }  
}

and we find what we’re looking for. Which is the **“balanceOf” mappings with a slot value of 3.**

This is all well and good; however, the true challenge arises when we encounter contracts employing **proxy patterns** and need to determine storage slot values for multiple other tokens.

Try running the above command with USDC (which is: **0xA0b86991c6218b36c1d19D4a2e9Eb0cE3606eB48**):

▶️ slither-read-storage 0xA0b86991c6218b36c1d19D4a2e9Eb0cE3606eB48 --json usdc_storage.json

This approach may encounter difficulties with tokens like USDC, as they employ a **proxy pattern contract**. Consequently, their storage is organized as expected, but their implementation code is deployed through another contract address, which we must discern independently.

**To get around this issue, we’ll turn to other ways of finding the storage slot values.**

First, we’ll examine how to employ **EVM tracing** in conjunction with **revm** to identify touched addresses and storage slots following the execution of a transaction:

1. **Using EVM tracing _(both on Geth and Parity)_**
2. **Using REVM**

Subsequently, we’ll delve into the process of identifying the implementation addresses of proxy contracts, enabling us to interact with tokens such as USDC.

## Storage slots, Storage slots

**1️⃣ Using EVM tracing to get storage slot values**

One way to figure out the storage slot value is with EVM tracing.

This is something we’ve already dealt with a little in the previous mempool watching blog post:

[

## How I spend my days Mempool watching (Part 1): Transaction Prediction through EVM Tracing

### How I started from bird watching to mempool watching, and how I’m building my sandwich bot now

medium.com



](https://medium.com/@solidquant/how-i-spend-my-days-mempool-watching-part-1-transaction-prediction-through-evm-tracing-77f4c99207f?source=post_page-----e01b5b0421e4---------------------------------------)

We’ll do the same thing again, but with an extra touch this time, we’ll:

- retrieve the **addresses that were touched** while running the transaction,
- retrieve all the **storage slots that changed states** from each of these touched addresses.

Create a new file in the **src** directory **(src/trace.rs)**:

use anyhow::Result;  
use ethers::prelude::*;  
use ethers_providers::Middleware;  
use std::sync::Arc;  
  
pub async fn get_state_diff<M: Middleware + 'static>(  
    provider: Arc<M>,  
    tx: Eip1559TransactionRequest,  
    block_number: U64,  
) -> Result<GethTrace> {  
    let trace = provider  
        .debug_trace_call(  
            tx,  
            Some(block_number.into()),  
            GethDebugTracingCallOptions {  
                tracing_options: GethDebugTracingOptions {  
                    disable_storage: None,  
                    disable_stack: None,  
                    enable_memory: None,  
                    enable_return_data: None,  
                    tracer: Some(GethDebugTracerType::BuiltInTracer(  
                        GethDebugBuiltInTracerType::PreStateTracer,  
                    )),  
                    tracer_config: None,  
                    timeout: None,  
                },  
                state_overrides: None,  
            },  
        )  
        .await?;  
  
    Ok(trace)  
}

And update the **(src/lib.rs)** file:

pub mod revm_examples;  
pub mod constants;  
pub mod utils;  
pub mod trace;

We create a simple function _(get_state_diff)_ calling the **debug_traceCall** RPC endpoint on Geth nodes.

_For readers running personal Parity style nodes like Erigon, they can reference the below code to achieve something similar with_ **_trace endpoints_**_:_

[

## evm-tracing-samples/rust/src/trace.rs at main · solidquant/evm-tracing-samples

### Sample scripts using EVM Tracing in Python and Rust - evm-tracing-samples/rust/src/trace.rs at main ·…

github.com



](https://github.com/solidquant/evm-tracing-samples/blob/main/rust/src/trace.rs?source=post_page-----e01b5b0421e4---------------------------------------)

The above code looks pretty intimidating, but it’s nothing but a simple wrapper around creating the below request frame:

{  
    'id': 1,  
    'method': 'debug_traceCall',  
    'jsonrpc': '2.0',  
    'params': [  
        tx_hash,  
        {'tracer': 'prestateTracer'}  
    ]  
}

[

## debug_traceCall RPC Method | Ethereum Documentation

### Top projects & businesses trust QuickNode for fast, reliable Web3 infrastructure. Instantly access 20 chains across a…

www.quicknode.com



](https://www.quicknode.com/docs/ethereum/debug_traceCall?source=post_page-----e01b5b0421e4---------------------------------------)

As you can see, we’re using the **prestateTracer** to trace any state changes with the transaction call.

Go to **(src/revm_examples.rs)** now and add the following before we can see what this function does line by line:

// previous imports  
  
// add these imports 👇  
use crate::constants::SIMULATOR_CODE;  
use crate::trace::get_state_diff;  
  
// create_evm_instance  
  
// evm_env_setup  
  
// get_token_balance  
  
  
// add this 👇  
pub async fn geth_and_revm_tracing<M: Middleware + 'static>(  
    evm: &mut EVM<InMemoryDB>,  
    provider: Arc<M>,  
    token: H160,  
    account: H160,  
) -> Result<i32> {  
    let erc20_abi = BaseContract::from(parse_abi(&[  
        "function balanceOf(address) external view returns (uint256)",  
    ])?);  
    let calldata = erc20_abi.encode("balanceOf", account)?;  
  
    let block = provider  
        .get_block(BlockNumber::Latest)  
        .await?  
        .ok_or(anyhow!("failed to retrieve block"))?;  
    let nonce = provider  
        .get_transaction_count(account, Some(BlockId::Number(BlockNumber::Latest)))  
        .await?;  
    let chain_id = provider.get_chainid().await?;  
  
    let tx = Eip1559TransactionRequest {  
        chain_id: Some(chain_id.as_u64().into()),  
        nonce: Some(nonce),  
        from: Some(account),  
        to: Some(NameOrAddress::Address(token)),  
        gas: None,  
        value: None,  
        data: Some(calldata),  
        max_priority_fee_per_gas: None,  
        max_fee_per_gas: None,  
        access_list: AccessList::default(),  
    };  
    let geth_trace = get_state_diff(provider.clone(), tx, block.number.unwrap()).await?;  
    let prestate = match geth_trace {  
        GethTrace::Known(known) => match known {  
            GethTraceFrame::PreStateTracer(prestate) => match prestate {  
                PreStateFrame::Default(prestate_mode) => Some(prestate_mode),  
                _ => None,  
            },  
            _ => None,  
        },  
        _ => None,  
    }  
    .unwrap();  
    let geth_touched_accs = prestate.0.keys();  
    info!("Geth trace: {:?}", geth_touched_accs);  
  
    let token_acc_state = prestate.0.get(&token).ok_or(anyhow!("no token key"))?;  
    let token_touched_storage = token_acc_state  
        .storage  
        .clone()  
        .ok_or(anyhow!("no storage values"))?;  
  
    for i in 0..20 {  
        let slot = keccak256(&abi::encode(&[  
            abi::Token::Address(account.into()),  
            abi::Token::Uint(U256::from(i)),  
        ]));  
        info!("{} {:?}", i, slot);  
        match token_touched_storage.get(&slot.into()) {  
            Some(_) => {  
                info!("Balance storage slot: {:?} ({:?})", i, slot);  
                return Ok(i);  
            }  
            None => {}  
        }  
    }  
  
    Ok(0)  
}

1. First, we create an Eip1559 transaction object:

let erc20_abi = BaseContract::from(parse_abi(&[  
    "function balanceOf(address) external view returns (uint256)",  
])?);  
let calldata = erc20_abi.encode("balanceOf", account)?;  
  
let block = provider  
    .get_block(BlockNumber::Latest)  
    .await?  
    .ok_or(anyhow!("failed to retrieve block"))?;  
let nonce = provider  
    .get_transaction_count(account, Some(BlockId::Number(BlockNumber::Latest)))  
    .await?;  
let chain_id = provider.get_chainid().await?;  
  
let tx = Eip1559TransactionRequest {  
    chain_id: Some(chain_id.as_u64().into()),  
    nonce: Some(nonce),  
    from: Some(account),  
    to: Some(NameOrAddress::Address(token)),  
    gas: None,  
    value: None,  
    data: Some(calldata),  
    max_priority_fee_per_gas: None,  
    max_fee_per_gas: None,  
    access_list: AccessList::default(),  
};

This isn’t too difficult. We retrieve block, nonce, chain_id information through RPC calls, and fill up the **Eip1559TransactionRequest** struct accordingly.

2. Next, we run **debug_traceCall** and get all the touched addresses by running the **“balanceOf”** function. We can expect to see our EVM machine touching the **balances mapping of the ERC-20 token contract**.

let geth_trace = get_state_diff(provider.clone(), tx, block.number.unwrap()).await?;  
let prestate = match geth_trace {  
    GethTrace::Known(known) => match known {  
        GethTraceFrame::PreStateTracer(prestate) => match prestate {  
            PreStateFrame::Default(prestate_mode) => Some(prestate_mode),  
            _ => None,  
        },  
        _ => None,  
    },  
    _ => None,  
}  
.unwrap();  
let geth_touched_accs = prestate.0.keys();  
info!("Geth trace: {:?}", geth_touched_accs);

Let’s see what this will output. Go to **(src/main.rs)**:

// previous imports  
  
// add this 👇  
use revm_is_all_you_need::revm_examples::{  
    create_evm_instance, evm_env_setup, get_token_balance, geth_and_revm_tracing  
};  
  
#[tokio::main]  
async fn main() -> Result<()> {  
    // previous code here  
  
    // add this 👇  
    let env = Env::new();  
    let ws = Ws::connect(&env.wss_url).await.unwrap();  
    let provider = Arc::new(Provider::new(ws));  
  
    match geth_and_revm_tracing(&mut evm, provider.clone(), weth, user).await {  
        Ok(_) => {}  
        Err(e) => info!("Tracing error: {e:?}"),  
    }  
  
    Ok(())  
}

and run the code:

> [14:27:07][INFO] Geth trace: **[0x95222290dd7278aa3ddd389cc1e1d165cc4bafe5, 0xc02aaa39b223fe8d0a0e5c4f27ead9083c756cc2, 0xe2b5a9c1e325511a227ef527af38c3a7b65afa1d]**

We can see that running “balanceOf” function on WETH touches the above three addresses, and we’re interested in **0xc02aaa39b223fe8d0a0e5c4f27ead9083c756cc2** address.

[](https://medium.com/write?source=promotion_paragraph---post_body_banner_home_for_stories_blocks--e01b5b0421e4---------------------------------------)

3. Lastly, we filter out the WETH contract from our **prestate** BTreeMap by doing:

let token_acc_state = prestate.0.get(&token).ok_or(anyhow!("no token key"))?;  
let token_touched_storage = token_acc_state  
    .storage  
    .clone()  
    .ok_or(anyhow!("no storage values"))?;  
  
for i in 0..20 {  
    let slot = keccak256(&abi::encode(&[  
        abi::Token::Address(account.into()),  
        abi::Token::Uint(U256::from(i)),  
    ]));  
    info!("{} {:?}", i, slot);  
    match token_touched_storage.get(&slot.into()) {  
        Some(_) => {  
            info!("Balance storage slot: {:?} ({:?})", i, slot);  
            return Ok(i);  
        }  
        None => {}  
    }  
}

We retrieve all the storage slots that were touched from the WETH contract, which can be accessed through the **storage** field of **AccountState** retrievable by doing **prestate.0.get(&token):**

Press enter or click to view image in full size

![](https://miro.medium.com/v2/resize:fit:1400/1*pyXFr2fsD56hUx6xNShQQg.png)

We start looping through numbers from 0 to 20 and calculate the storage slot of our desired **“balance”** mappings holding the balance of our targeted **account**. And we get something like this:

Press enter or click to view image in full size

![](https://miro.medium.com/v2/resize:fit:1400/1*KMvxB7ikL8YssBCFpD3T7g.png)

**_We can see that the balance mappings storage slot value of WETH is: 3_**

That’s exactly what we wanted to see.

**2️⃣ Using REVM to get storage slot values**

That was fun, but let’s not forget that this is an **revm** tutorial, not a Geth tutorial. The reason why I showed you this is to illustrate the idea that the same results can be reproduced using **revm** alone.

Now’s a good time to recall that our first attempt at calling the **“balanceOf”** function using **revm** failed, because we were using a clean slate version of the EVM instance. This will go differently if we were to deploy the token contract before calling the function.

To do this, we will create another function in our **(src/revm_examples.rs)**:

// previous code here...  
  
// add this 👇  
pub async fn revm_contract_deploy_and_tracing<M: Middleware + 'static>(  
    evm: &mut EVM<InMemoryDB>,  
    provider: Arc<M>,  
    token: H160,  
    account: H160,  
) -> Result<i32> {  
    // deploy contract to EVM  
    let block = provider  
        .get_block(BlockNumber::Latest)  
        .await?  
        .ok_or(anyhow!("failed to retrieve block"))?;  
  
    let mut ethersdb = EthersDB::new(provider.clone(), Some(block.number.unwrap().into())).unwrap();  
  
    let token_acc_info = ethersdb.basic(token.into()).unwrap().unwrap();  
    evm.db  
        .as_mut()  
        .unwrap()  
        .insert_account_info(token.into(), token_acc_info);  
  
    let erc20_abi = BaseContract::from(parse_abi(&[  
        "function balanceOf(address) external view returns (uint256)",  
    ])?);  
    let calldata = erc20_abi.encode("balanceOf", account)?;  
  
    evm.env.tx.caller = account.into();  
    evm.env.tx.transact_to = TransactTo::Call(token.into());  
    evm.env.tx.data = calldata.0.clone();  
  
    let result = match evm.transact_ref() {  
        Ok(result) => result,  
        Err(e) => return Err(anyhow!("EVM call failed: {e:?}")),  
    };  
    let token_b160: B160 = token.into();  
    let token_acc = result.state.get(&token_b160).unwrap();  
    let token_touched_storage = token_acc.storage.clone();  
    info!("Touched storage slots: {:?}", token_touched_storage);  
  
    for i in 0..20 {  
        let slot = keccak256(&abi::encode(&[  
            abi::Token::Address(account.into()),  
            abi::Token::Uint(U256::from(i)),  
        ]));  
        let slot: rU256 = U256::from(slot).into();  
        match token_touched_storage.get(&slot) {  
            Some(_) => {  
                info!("Balance storage slot: {:?} ({:?})", i, slot);  
                return Ok(i);  
            }  
            None => {}  
        }  
    }  
  
    Ok(0)  
}

The code will start to look familiar to some readers, but don’t worry, I’m still going to go over them line by line, because that’s what I like to do.

This time, we’re going to using **EthersDB** from **revm** to retrieve basic account information about the token we want to deploy and inject that into the EVM DB. **Note** that this will make _three async calls_ to our RPC endpoint, which can be seen from the function definition:

let f = async {  
    let nonce = self.client.get_transaction_count(add, self.block_number);  
    let balance = self.client.get_balance(add, self.block_number);  
    let code = self.client.get_code(add, self.block_number);  
    tokio::join!(nonce, balance, code)  
};

_This is why using this method without caution can actually take a very long time._

Let’s keep going.

First, we create an EthersDB instance:

// deploy contract to EVM  
let block = provider  
    .get_block(BlockNumber::Latest)  
    .await?  
    .ok_or(anyhow!("failed to retrieve block"))?;  
  
let mut ethersdb = EthersDB::new(provider.clone(), Some(block.number.unwrap().into())).unwrap();  
  
let token_acc_info = ethersdb.basic(token.into()).unwrap().unwrap();  
evm.db  
    .as_mut()  
    .unwrap()  
    .insert_account_info(token.into(), token_acc_info);

Running the **basic** function using **EthersDB** will return an **AccountInstance** struct that looks like:

Press enter or click to view image in full size

![](https://miro.medium.com/v2/resize:fit:1400/1*ZhyYL0K-eIRQi7Wnm3vt0g.png)

We can see that we can retrieve the bytecode deployed to the target account using this method from the mainnet. So simply injecting this account information into EVM DB is the equivalent of deploying smart contracts to that address.

Next, we try making the **“balanceOf”** call to the token address again this time:

let erc20_abi = BaseContract::from(parse_abi(&[  
    "function balanceOf(address) external view returns (uint256)",  
])?);  
let calldata = erc20_abi.encode("balanceOf", account)?;  
  
evm.env.tx.caller = account.into();  
evm.env.tx.transact_to = TransactTo::Call(token.into());  
evm.env.tx.data = calldata.0.clone();  
  
let result = match evm.transact_ref() {  
    Ok(result) => result,  
    Err(e) => return Err(anyhow!("EVM call failed: {e:?}")),  
};

We should all be very familiar with how we call transactions using the EVM instance by now. But this time, we’re going to access the touched storage slot values returned by **revm** by doing:

let token_b160: B160 = token.into();  
let token_acc = result.state.get(&token_b160).unwrap();  
let token_touched_storage = token_acc.storage.clone();  
info!("Touched storage slots: {:?}", token_touched_storage);

Running this will result in:

> [16:05:56][INFO] Touched storage slots: {**0xb4dd106494c3538bbf85de3412632df633f8b5c7be6ffb607fdbabd4700d7d57_U256**: StorageSlot { original_value: 0x0_U256, present_value: 0x0_U256 }}

Voila, there we have it. We can see that we get something very similar to what we had with Geth EVM tracing.

We can see what storage slots were touched, and how the values changed (pre/post state).

So, we do the same thing that we did with Geth EVM traces:

for i in 0..20 {  
    let slot = keccak256(&abi::encode(&[  
        abi::Token::Address(account.into()),  
        abi::Token::Uint(U256::from(i)),  
    ]));  
    let slot: rU256 = U256::from(slot).into();  
    match token_touched_storage.get(&slot) {  
        Some(_) => {  
            info!("Balance storage slot: {:?} ({:?})", i, slot);  
            return Ok(i);  
        }  
        None => {}  
    }  
}

Go over to **(src/main.rs)**:

// previous imports  
  
// fix this 👇  
use revm_is_all_you_need::revm_examples::{  
    create_evm_instance, evm_env_setup, get_token_balance, geth_and_revm_tracing,  
    revm_contract_deploy_and_tracing,  
};  
  
#[tokio::main]  
async fn main() -> Result<()> {  
    // previous code here...  
  
    // add this 👇  
    match revm_contract_deploy_and_tracing(&mut evm, provider.clone(), weth, user).await {  
        Ok(_) => {}  
        Err(e) => info!("Tracing error: {e:?}"),  
    }

Doing this, and running **cargo run,** we get the same result as before:

> [16:05:56][INFO] Balance storage slot: **3** (0xb4dd106494c3538bbf85de3412632df633f8b5c7be6ffb607fdbabd4700d7d57_U256)

## Proxy patterns, Proxy patterns

Now that we know how to use **revm** as a way of getting storage value changes, let’s look at an edge case where we’re trying to deploy a contract using proxy patterns.

One such example is the USDC token. In the example above, we were dealing with the WETH token, which doesn’t use the proxy pattern, so we could deploy the token contract as easily as doing:

// deploy contract to EVM  
let block = provider  
    .get_block(BlockNumber::Latest)  
    .await?  
    .ok_or(anyhow!("failed to retrieve block"))?;  
  
let mut ethersdb = EthersDB::new(provider.clone(), Some(block.number.unwrap().into())).unwrap();  
  
let token_acc_info = ethersdb.basic(token.into()).unwrap().unwrap();  
evm.db  
    .as_mut()  
    .unwrap()  
    .insert_account_info(token.into(), token_acc_info);

However, if we do this with USDC, we can see from the contract definition in Etherscan, that we’ll get the bytecode of something called the UpgradeabilityProxy contract:

Press enter or click to view image in full size

![](https://miro.medium.com/v2/resize:fit:1400/1*ITr1-oI7lNFtA3jhp69mgQ.png)

This is quite convenient as we can see right away that we can gain access to the implementation contract address via the IMPLEMENTATION_SLOT variable.

But the true nightmare begins when we realize that there are numerous ways to implement the proxy pattern, and that the UpgradeabilityProxy is just one of many ways to do it.

So we make a simple function that can cover 4 different proxy patterns (EIP-1967 logic/beacon, OpenZeppelin implementation, and EIP-1822 logic) in a new file we’ll name **(src/tokens.rs)**:

use anyhow::Result;  
use ethers::prelude::*;  
use ethers_core::types::{BlockId, BlockNumber, TxHash, H160, U256};  
use std::sync::Arc;  
use tokio::task::JoinSet;  
  
use crate::constants::ZERO_ADDRESS;  
  
pub async fn get_implementation<M: Middleware + 'static>(  
    provider: Arc<M>,  
    token: H160,  
    block_number: U64,  
) -> Result<Option<H160>> {  
    // adapted from: https://github.com/gnosis/evm-proxy-detection/blob/main/src/index.ts  
    let eip_1967_logic_slot =  
        U256::from("0x360894a13ba1a3210667c828492db98dca3e2076cc3735a920a3ca505d382bbc");  
    let eip_1967_beacon_slot =  
        U256::from("0xa3f0ad74e5423aebfd80d3ef4346578335a9a72aeaee59ff6cb3582b35133d50");  
    let open_zeppelin_implementation_slot =  
        U256::from("0x7050c9e0f4ca769c69bd3a8ef740bc37934f8e2c036e5a723fd8ee048ed3f8c3");  
    let eip_1822_logic_slot =  
        U256::from("0xc5f16f0fcc639fa48a6947836d9850f504798523bf8c9a3a87d5876cf622bcf7");  
  
    let implementation_slots = vec![  
        eip_1967_logic_slot,  
        eip_1967_beacon_slot,  
        open_zeppelin_implementation_slot,  
        eip_1822_logic_slot,  
    ];  
  
    let mut set = JoinSet::new();  
  
    for slot in implementation_slots {  
        let _provider = provider.clone();  
        let fut = tokio::spawn(async move {  
            _provider  
                .get_storage_at(token, TxHash::from_uint(&slot), Some(block_number.into()))  
                .await  
        });  
        set.spawn(fut);  
    }  
  
    while let Some(res) = set.join_next().await {  
        let out = res???;  
        let implementation = H160::from(out);  
        if implementation != *ZERO_ADDRESS {  
            return Ok(Some(implementation));  
        }  
    }  
  
    Ok(None)  
}

And update **(src/lib.rs)**:

pub mod revm_examples;  
pub mod constants;  
pub mod utils;  
pub mod trace;  
pub mod tokens;

This is a simplied version of:

[

## evm-proxy-detection/src/index.ts at main · gnosis/evm-proxy-detection

### Detect proxy contracts and their target addresses using an EIP-1193 compatible JSON-RPC request function …

github.com



](https://github.com/gnosis/evm-proxy-detection/blob/main/src/index.ts?source=post_page-----e01b5b0421e4---------------------------------------)

so readers who want a more complete way of handling this can refer to the above link.

Using the **“get_implementation”** function, we’re basically calling the four different slots with **eth_getStorageAt** call to access each slot values, and see if they are storing the implementation address. If the returned value isn’t ZERO ADDRESS, we know that the implementation contract’s bytecode is behind a proxy pattern, and we have to use that address to retrieve the code for our target contract instead.

Kind of confusing, right?

## 🔮 **REVM is all you need**

We’ve come a long way. Let’s quickly wrap up what we have covered so far:

- we know how to create the **EVM instance**,
- we know how to use **revm** as we would with Geth **EVM tracing**,
- we know how to get the **proxy implementation** address,
- we know how to **inject storage values** using the storage slot

And using these techniques, we’ll finally simulate our **v2SimulateSwap** function in our **Simulator** contract.

Let’s hop on over to the **(src/revm_examples.rs)** and add:

// previous code here...  
  
// add this 👇  
pub async fn revm_v2_simulate_swap<M: Middleware + 'static>(  
    evm: &mut EVM<InMemoryDB>,  
    provider: Arc<M>,  
    account: H160,  
    factory: H160,  
    target_pair: H160,  
    input_token: H160,  
    output_token: H160,  
    input_balance_slot: i32,  
    output_balance_slot: i32,  
    input_token_implementation: Option<H160>,  
    output_token_implementation: Option<H160>,  
) -> Result<(U256, U256)> {  
    Ok((U256::zero(), U256::zero())) // 👈 placeholder for now, will update later  
}

We’ll implement this function one line at a time.

Let’s quickly recall what the **v2SimulateSwap** function signature was:

function v2SimulateSwap(    uint256 amountIn,  
    address targetPair,  
    address inputToken,  
    address outputToken) external returns (uint256 amountOut, uint256 realAfterBalance)

With this in mind, we first of all call for the latest block information using our provider:

// previous code here...  
  
pub async fn revm_v2_simulate_swap<M: Middleware + 'static>(  
    evm: &mut EVM<InMemoryDB>,  
    provider: Arc<M>,  
    account: H160,  
    factory: H160,  
    target_pair: H160,  
    input_token: H160,  
    output_token: H160,  
    input_balance_slot: i32,  
    output_balance_slot: i32,  
    input_token_implementation: Option<H160>,  
    output_token_implementation: Option<H160>,  
) -> Result<(U256, U256)> {  
    // add this 👇  
    let block = provider  
        .get_block(BlockNumber::Latest)  
        .await?  
        .ok_or(anyhow!("failed to retrieve block"))?;  
  
  
    Ok((U256::zero(), U256::zero())) // 👈 placeholder for now, will update later  
}

Next, we create the **EthersDB**, so that we can make calls to our node endpoints:

// previous code here...  
  
pub async fn revm_v2_simulate_swap<M: Middleware + 'static>(  
    // params...  
) -> Result<(U256, U256)> {  
    let block = provider  
        .get_block(BlockNumber::Latest)  
        .await?  
        .ok_or(anyhow!("failed to retrieve block"))?;  
  
    // add this 👇  
    let mut ethersdb = EthersDB::new(provider.clone(), Some(block.number.unwrap().into())).unwrap();  
  
    Ok((U256::zero(), U256::zero())) // 👈 placeholder for now, will update later  
}

Our **EthersDB** will make calls to the latest block data.

We now, extract out the mutable reference of our DB as below:

// previous code here...  
  
pub async fn revm_v2_simulate_swap<M: Middleware + 'static>(  
    // params...  
) -> Result<(U256, U256)> {  
    let block = provider  
        .get_block(BlockNumber::Latest)  
        .await?  
        .ok_or(anyhow!("failed to retrieve block"))?;  
  
    let mut ethersdb = EthersDB::new(provider.clone(), Some(block.number.unwrap().into())).unwrap();  
  
    // add this 👇  
    let db = evm.db.as_mut().unwrap();  
  
    Ok((U256::zero(), U256::zero())) // 👈 placeholder for now, will update later  
}

this can be accessible from our EVM instance. We’ll be using this to deploy contract and inject storage values.

### ✅ Set user balance

Then, we create the main user account and feed it the balance of 10 ETH:

// previous code here...  
  
pub async fn revm_v2_simulate_swap<M: Middleware + 'static>(  
    // params...  
) -> Result<(U256, U256)> {  
    let block = provider  
        .get_block(BlockNumber::Latest)  
        .await?  
        .ok_or(anyhow!("failed to retrieve block"))?;  
  
    let mut ethersdb = EthersDB::new(provider.clone(), Some(block.number.unwrap().into())).unwrap();  
  
    let db = evm.db.as_mut().unwrap();  
  
    // add this 👇  
    let ten_eth = rU256::from(10)  
        .checked_mul(rU256::from(10).pow(rU256::from(18)))  
        .unwrap();  
  
    // Set user: give the user enough ETH to pay for gas  
    let user_acc_info = AccountInfo::new(ten_eth, 0, Bytecode::default());  
    db.insert_account_info(account.into(), user_acc_info);  
  
    Ok((U256::zero(), U256::zero())) // 👈 placeholder for now, will update later  
}

Now that we have our main user account setup, we need to deploy our Simulator contract to the EVM DB (we give it a random address):

// previous code here...  
  
pub async fn revm_v2_simulate_swap<M: Middleware + 'static>(  
    // params...  
) -> Result<(U256, U256)> {  
    let block = provider  
        .get_block(BlockNumber::Latest)  
        .await?  
        .ok_or(anyhow!("failed to retrieve block"))?;  
  
    let mut ethersdb = EthersDB::new(provider.clone(), Some(block.number.unwrap().into())).unwrap();  
  
    let db = evm.db.as_mut().unwrap();  
  
    let ten_eth = rU256::from(10)  
        .checked_mul(rU256::from(10).pow(rU256::from(18)))  
        .unwrap();  
  
    // Set user: give the user enough ETH to pay for gas  
    let user_acc_info = AccountInfo::new(ten_eth, 0, Bytecode::default());  
    db.insert_account_info(account.into(), user_acc_info);  
  
    // add this 👇  
    // Deploy Simulator contract  
    let simulator_address = H160::from_str("0xF2d01Ee818509a9540d8324a5bA52329af27D19E").unwrap();  
    let simulator_acc_info = AccountInfo::new(  
        rU256::ZERO,  
        0,  
        Bytecode::new_raw((*SIMULATOR_CODE.0).into()),  
    );  
    db.insert_account_info(simulator_address.into(), simulator_acc_info);  
  
    Ok((U256::zero(), U256::zero())) // 👈 placeholder for now, will update later  
}

Since we need to deploy contracts for the input token, output token, and the Uniswap V2 pair for those two tokens, we simply use the **Uniswap V2 Factory** contract to create a new pair:

// previous code here...  
  
pub async fn revm_v2_simulate_swap<M: Middleware + 'static>(  
    // params...  
) -> Result<(U256, U256)> {  
    // previous code here...  
  
    // add this 👇  
    // Deploy necessary contracts to simulate Uniswap V2 swap  
    let input_token_address = match input_token_implementation {  
        Some(implementation) => implementation,  
        None => input_token,  
    };  
    let output_token_address = match output_token_implementation {  
        Some(implementation) => implementation,  
        None => output_token,  
    };  
    let input_token_acc_info = ethersdb.basic(input_token_address.into()).unwrap().unwrap();  
    let output_token_acc_info = ethersdb  
        .basic(output_token_address.into())  
        .unwrap()  
        .unwrap();  
    let factory_acc_info = ethersdb.basic(factory.into()).unwrap().unwrap();  
  
    db.insert_account_info(input_token.into(), input_token_acc_info);  
    db.insert_account_info(output_token.into(), output_token_acc_info);  
    db.insert_account_info(factory.into(), factory_acc_info);  
  
    Ok((U256::zero(), U256::zero())) // 👈 placeholder for now, will update later  
}

But remember, that our tokens may be hiding behind a proxy pattern contract, so we receive Optional input_token_implementation / output_token_implementation values, and use those addresses as the input for our EthersDB **basic** call if it isn’t a None value. With this we have deployed three contracts:

- **input token,**
- **output token,**
- **Uniswap V2 Factory**

### ✅ Create new pair using Uniswap V2 Factory

This time, we’ll try creating a new pair with a call to our Factory contract:

// previous code here...  
  
// add this 👇  
pub fn get_tx_result(result: ExecutionResult) -> Result<TxResult> {  
    let output = match result {  
        ExecutionResult::Success {  
            gas_used,  
            gas_refunded,  
            output,  
            logs,  
            ..  
        } => match output {  
            Output::Call(o) => TxResult {  
                output: o,  
                logs: Some(logs),  
                gas_used,  
                gas_refunded,  
            },  
            Output::Create(o, _) => TxResult {  
                output: o,  
                logs: Some(logs),  
                gas_used,  
                gas_refunded,  
            },  
        },  
        ExecutionResult::Revert { gas_used, output } => {  
            return Err(anyhow!(  
                "EVM REVERT: {:?} / Gas used: {:?}",  
                output,  
                gas_used  
            ))  
        }  
        ExecutionResult::Halt { reason, .. } => return Err(anyhow!("EVM HALT: {:?}", reason)),  
    };  
  
    Ok(output)  
}  
  
pub async fn revm_v2_simulate_swap<M: Middleware + 'static>(  
    // params...  
) -> Result<(U256, U256)> {  
    // previous code here...  
  
    // add this 👇  
    // Deploy pair contract using factory  
    let factory_abi = BaseContract::from(parse_abi(&[  
        "function createPair(address,address) external returns (address)",  
    ])?);  
    let calldata = factory_abi.encode("createPair", (input_token, output_token))?;  
  
    let gas_price = rU256::from(100)  
        .checked_mul(rU256::from(10).pow(rU256::from(9)))  
        .unwrap();  
  
    // Create a pair contract using the factory contract  
    let create_pair_tx = TxEnv {  
        caller: account.into(),  
        gas_limit: 5000000,  
        gas_price: gas_price,  
        gas_priority_fee: None,  
        transact_to: TransactTo::Call(factory.into()),  
        value: rU256::ZERO,  
        data: calldata.0,  
        chain_id: None,  
        nonce: None,  
        access_list: Default::default(),  
    };  
    evm.env.tx = create_pair_tx;  
  
    let result = match evm.transact_commit() {  
        Ok(result) => result,  
        Err(e) => return Err(anyhow!("EVM call failed: {:?}", e)),  
    };  
    let result = get_tx_result(result)?;  
    let pair_address: H160 = factory_abi.decode_output("createPair", result.output)?;  
    info!("Pair created: {:?}", pair_address);  
  
    Ok((U256::zero(), U256::zero())) // 👈 placeholder for now, will update later  
}

Note that we’re using 100 GWEI as our gas_price.

We are also using a somewhat different way of running our transaction. We do it using the TxEnv struct this time. Other than that, the syntax should look familiar to you.

We retrieve TxResult by making a transact_commit call using our EVM instance. Doing this, we can see that our pair has been created successfully.

### ✅ Get logs after running transaction

The nice thing about using **revm** is that it really is the same as running your transactions on the Ethereum node. Running a transaction will also give you access to the output logs, which we get by doing:

// previous code here...  
  
pub async fn revm_v2_simulate_swap<M: Middleware + 'static>(  
    // params...  
) -> Result<(U256, U256)> {  
    // previous code here...  
  
    // add this 👇  
    // parse PairCreated event to get token0 / token1  
    let pair_created_log = &result.logs.unwrap()[0];  
    let token0: B160 = pair_created_log.topics[1].into();  
    let token1: B160 = pair_created_log.topics[2].into();  
    info!("Token 0: {:?} / Token 1: {:?}", token0, token1);  
  
    // Check if the target_pair is equal to the pair created address  
    assert_eq!(target_pair, pair_address);  
  
    Ok((U256::zero(), U256::zero())) // 👈 placeholder for now, will update later  
}

We check that **token0** and **token1** are values we expected.

### ✅ Inject reserves to the pair

With our pair contract ready, we retrieve the original reserves data from our node:

// previous code here...  
  
pub async fn revm_v2_simulate_swap<M: Middleware + 'static>(  
    // params...  
) -> Result<(U256, U256)> {  
    // previous code here...  
  
    // add this 👇  
    // There're no reserves in the pool, so we inject the reserves that we retrieve with ethersdb  
    // The storage slot of reserves is: 8  
    let db = evm.db.as_mut().unwrap();  
    let reserves_slot = rU256::from(8);  
    let original_reserves = ethersdb  
        .storage(pair_address.into(), reserves_slot)  
        .unwrap();  
    db.insert_account_storage(pair_address.into(), reserves_slot, original_reserves)?;  
  
    // Check that the reserves are set correctly  
    let pair_abi = BaseContract::from(parse_abi(&[  
        "function getReserves() external view returns (uint112,uint112,uint32)",  
    ])?);  
    let calldata = pair_abi.encode("getReserves", ())?;  
    let get_reserves_tx = TxEnv {  
        caller: account.into(),  
        gas_limit: 5000000,  
        gas_price: gas_price,  
        gas_priority_fee: None,  
        transact_to: TransactTo::Call(target_pair.into()),  
        value: rU256::ZERO,  
        data: calldata.0,  
        chain_id: None,  
        nonce: None,  
        access_list: Default::default(),  
    };  
    evm.env.tx = get_reserves_tx;  
  
    let result = match evm.transact_ref() {  
        Ok(result) => result,  
        Err(e) => return Err(anyhow!("EVM call failed: {:?}", e)),  
    };  
    let result = get_tx_result(result.result)?;  
    let reserves: (U256, U256, U256) = pair_abi.decode_output("getReserves", result.output)?;  
    info!("Pair reserves: {:?}", reserves);  
  
    Ok((U256::zero(), U256::zero())) // 👈 placeholder for now, will update later  
}

After doing so, we try calling the **“getReserves”** function to see that the pair’s reserves have been set correctly. _(using_ **_transact_ref_** _because we don’t make any stateful changes)_

We’re almost done.

### ✅ Setting pair input/output token balance

If we study the Uniswap V2 Pair **“swap”** function:

Press enter or click to view image in full size

![](https://miro.medium.com/v2/resize:fit:1400/1*HyG_YkQBeTyMXULy46g4cA.png)

we can notice that it retrieves the balance of **token0**, **token1** making calls to the token contracts.

This means that our newly deployed pair contract has to have real token balances to perform a real swap. Lucky for us, we already know how to do this:

// previous code here...  
  
pub async fn revm_v2_simulate_swap<M: Middleware + 'static>(  
    // params...  
) -> Result<(U256, U256)> {  
    // previous code here...  
  
    // add this 👇  
    // We actually have to feed the input/output token balance to pair contract (to perform real swaps)  
    let db = evm.db.as_mut().unwrap();  
  
    let (balance_slot_0, balance_slot_1) = if token0 == input_token.into() {  
        (input_balance_slot, output_balance_slot)  
    } else {  
        (output_balance_slot, input_balance_slot)  
    };  
    info!(  
        "Balance slot 0: {:?} / slot 1: {:?}",  
        balance_slot_0, balance_slot_1  
    );  
  
    let pair_token0_slot = keccak256(&abi::encode(&[  
        abi::Token::Address(target_pair.into()),  
        abi::Token::Uint(U256::from(balance_slot_0)),  
    ]));  
    db.insert_account_storage(token0, pair_token0_slot.into(), reserves.0.into())?;  
  
    let pair_token1_slot = keccak256(&abi::encode(&[  
        abi::Token::Address(target_pair.into()),  
        abi::Token::Uint(U256::from(balance_slot_1)),  
    ]));  
    db.insert_account_storage(token1, pair_token1_slot.into(), reserves.1.into())?;  
  
    // Check that balance is set correctly  
    let token_abi = BaseContract::from(parse_abi(&[  
        "function balanceOf(address) external view returns (uint256)",  
    ])?);  
    for token in vec![token0, token1] {  
        let calldata = token_abi.encode("balanceOf", target_pair)?;  
        evm.env.tx.caller = account.into();  
        evm.env.tx.transact_to = TransactTo::Call(token);  
        evm.env.tx.data = calldata.0;  
        let result = match evm.transact_ref() {  
            Ok(result) => result,  
            Err(e) => return Err(anyhow!("EVM call failed: {:?}", e)),  
        };  
        let result = get_tx_result(result.result)?;  
        let balance: U256 = token_abi.decode_output("balanceOf", result.output)?;  
        info!("{:?}: {:?}", token, balance);  
    }  
  
    Ok((U256::zero(), U256::zero())) // 👈 placeholder for now, will update later  
}

After we set the token balances for the pair contract, we check to see that they’ve been set correctly by calling the **“balanceOf”** functions for each.

### ✅ Inject Simulator contract with input token balance

To perform a swap, we have to make sure that our **Simulator** has enough input token balance:

// previous code here...  
  
pub async fn revm_v2_simulate_swap<M: Middleware + 'static>(  
    // params...  
) -> Result<(U256, U256)> {  
    // previous code here...  
  
    // add this 👇  
    // feed simulator with input_token balance  
    let db = evm.db.as_mut().unwrap();  
  
    let slot_in = keccak256(&abi::encode(&[  
        abi::Token::Address(simulator_address.into()),  
        abi::Token::Uint(U256::from(input_balance_slot)),  
    ]));  
    db.insert_account_storage(input_token.into(), slot_in.into(), ten_eth)?;  
  
    Ok((U256::zero(), U256::zero())) // 👈 placeholder for now, will update later  
}

### ✅ v2SimulateSwap

We can finally perform our swap:

// previous code here...  
  
pub async fn revm_v2_simulate_swap<M: Middleware + 'static>(  
    // params...  
) -> Result<(U256, U256)> {  
    // previous code here...  
  
    // add this 👇  
    // run v2SimulateSwap  
    let amount_in = U256::from(1)  
        .checked_mul(U256::from(10).pow(U256::from(18)))  
        .unwrap();  
    let simulator_abi = BaseContract::from(  
        parse_abi(&[  
            "function v2SimulateSwap(uint256,address,address,address) external returns (uint256, uint256)",  
        ])?  
    );  
    let calldata = simulator_abi.encode(  
        "v2SimulateSwap",  
        (amount_in, target_pair, input_token, output_token),  
    )?;  
    let v2_simulate_swap_tx = TxEnv {  
        caller: account.into(),  
        gas_limit: 5000000,  
        gas_price: gas_price,  
        gas_priority_fee: None,  
        transact_to: TransactTo::Call(simulator_address.into()),  
        value: rU256::ZERO,  
        data: calldata.0,  
        chain_id: None,  
        nonce: None,  
        access_list: Default::default(),  
    };  
    evm.env.tx = v2_simulate_swap_tx;  
  
    let result = match evm.transact_commit() {  
        Ok(result) => result,  
        Err(e) => return Err(anyhow!("EVM call failed: {:?}", e)),  
    };  
    let result = get_tx_result(result)?;  
    let out: (U256, U256) = simulator_abi.decode_output("v2SimulateSwap", result.output)?;  
    info!("Amount out: {:?}", out);  
  
    Ok(out)  
}

We can try running this code from our **main.rs** file now **(src/main.rs)**:

// previous code here...  
  
// fix this 👇  
use revm_is_all_you_need::revm_examples::{  
    create_evm_instance, evm_env_setup, get_token_balance, geth_and_revm_tracing,  
    revm_contract_deploy_and_tracing, revm_v2_simulate_swap,  
};  
use revm_is_all_you_need::tokens::get_implementation;  
  
#[tokio::main]  
async fn main() -> Result<()> {  
    // previous code here...  
  
    // add this 👇  
    let block = provider  
        .get_block(BlockNumber::Latest)  
        .await  
        .unwrap()  
        .unwrap();  
  
    let uniswap_v2_factory = H160::from_str("0x5C69bEe701ef814a2B6a3EDD4B1652CB9cc5aA6f").unwrap();  
    let weth_usdt_pair = H160::from_str("0x0d4a11d5EEaaC28EC3F61d100daF4d40471f1852").unwrap();  
  
    let weth_balance_slot =  
        revm_contract_deploy_and_tracing(&mut evm, provider.clone(), weth, user)  
            .await  
            .unwrap();  
    let usdt_balance_slot =  
        revm_contract_deploy_and_tracing(&mut evm, provider.clone(), usdt, user)  
            .await  
            .unwrap();  
  
    let weth_implementation = get_implementation(provider.clone(), weth, block.number.unwrap())  
        .await  
        .unwrap();  
    let usdt_implementation = get_implementation(provider.clone(), usdt, block.number.unwrap())  
        .await  
        .unwrap();  
  
    info!("WETH proxy: {:?}", weth_implementation);  
    info!("USDT proxy: {:?}", usdt_implementation);  
  
    match revm_v2_simulate_swap(  
        &mut evm,  
        provider.clone(),  
        user,  
        uniswap_v2_factory,  
        weth_usdt_pair,  
        weth,  
        usdt,  
        weth_balance_slot,  
        usdt_balance_slot,  
        weth_implementation,  
        usdt_implementation,  
    )  
    .await  
    {  
        Ok(_) => {}  
        Err(e) => info!("v2SimulateSwap revm failed: {e:?}"),  
    }  
  
    Ok(())  
}

**Output:**

Press enter or click to view image in full size

![](https://miro.medium.com/v2/resize:fit:1400/1*6irtERmct5TrcKmFMpMYXQ.png)

We can see that swapping 1 WETH will give us:

> **[18:31:35][INFO] Amount out: (1588027787, 1588027787)**

USDT back. Which is the equivalent of:

> **1588027787 / 10 ** 6 = 1588.027787 USDT**

That seems about right 👍

## Okay, REVM isn’t all you need. Enter Foundry ❤️

If you’re like me, you’re probably wondering how you got here.

**I mean, all that work just to simulate a single swap?** What’s the point of using **revm** if a single swap will be so complicated.

That’s why you need to use a higher level interface to deal with all this.

**This is where foundry-evm can shine.**

The only thing you need to do to use **foundry-evm** is to switch the **EmptyDB** to **foundry-evm’s SharedBackend**. Now this is where all the magic happens.

Create a new file called **(src/foundry_examples.rs)**:

use anyhow::{anyhow, Result};  
use ethers::{  
    abi::{self, parse_abi},  
    prelude::*,  
    providers::Middleware,  
    types::{BlockNumber, H160, U256},  
};  
use foundry_evm::{  
    executor::{  
        fork::{BlockchainDb, BlockchainDbMeta, SharedBackend},  
        Bytecode, TransactTo, TxEnv,  
    },  
    revm::{  
        db::CacheDB,  
        primitives::{keccak256, AccountInfo, U256 as rU256},  
        EVM,  
    },  
};  
use log::info;  
use std::{collections::BTreeSet, str::FromStr, sync::Arc};  
  
use crate::constants::SIMULATOR_CODE;  
use crate::revm_examples::get_tx_result;  
  
pub async fn foundry_v2_simulate_swap<M: Middleware + 'static>(  
    provider: Arc<M>,  
    account: H160,  
    target_pair: H160,  
    input_token: H160,  
    output_token: H160,  
    input_balance_slot: i32,  
) -> Result<(U256, U256)> {  
    let block = provider  
        .get_block(BlockNumber::Latest)  
        .await?  
        .ok_or(anyhow!("failed to retrieve block"))?;  
  
    // pay attention to this part 👇  
    let shared_backend = SharedBackend::spawn_backend_thread(  
        provider.clone(),  
        BlockchainDb::new(  
            BlockchainDbMeta {  
                cfg_env: Default::default(),  
                block_env: Default::default(),  
                hosts: BTreeSet::from(["".to_string()]),  
            },  
            None,  
        ),  
        Some(block.number.unwrap().into()),  
    );  
    let db = CacheDB::new(shared_backend);  
  
    let mut evm = EVM::new();  
    evm.database(db);  
  
    // I'll reveal the rest of the code right below  
    // ...  
    Ok(out)  
}

As can be seen from above, we use something called the SharedBackend, which is a way of forking the mainnet in Foundry.

Remember how we had to inject values to whichever storage values we were going to use in our **revm** sample code?

Well, no more of that using the SharedBackend anymore. This backend system will **automatically retrieve all the account information and storage values for us from behind the scenes** whenever we try to access values that have not been called before.

How convenient is that!

So the rest of the code is something we’re already familiar with:

pub async fn foundry_v2_simulate_swap<M: Middleware + 'static>(  
    provider: Arc<M>,  
    account: H160,  
    target_pair: H160,  
    input_token: H160,  
    output_token: H160,  
    input_balance_slot: i32,  
) -> Result<(U256, U256)> {  
    let block = provider  
        .get_block(BlockNumber::Latest)  
        .await?  
        .ok_or(anyhow!("failed to retrieve block"))?;  
  
    let shared_backend = SharedBackend::spawn_backend_thread(  
        provider.clone(),  
        BlockchainDb::new(  
            BlockchainDbMeta {  
                cfg_env: Default::default(),  
                block_env: Default::default(),  
                hosts: BTreeSet::from(["".to_string()]),  
            },  
            None,  
        ),  
        Some(block.number.unwrap().into()),  
    );  
    let db = CacheDB::new(shared_backend);  
  
    let mut evm = EVM::new();  
    evm.database(db);  
  
    evm.env.cfg.limit_contract_code_size = Some(0x100000);  
    evm.env.cfg.disable_block_gas_limit = true;  
    evm.env.cfg.disable_base_fee = true;  
  
    evm.env.block.number = rU256::from(block.number.unwrap().as_u64() + 1);  
  
    let fork_db = evm.db.as_mut().unwrap();  
  
    let ten_eth = rU256::from(10)  
        .checked_mul(rU256::from(10).pow(rU256::from(18)))  
        .unwrap();  
  
    // Set user: give the user enough ETH to pay for gas  
    let user_acc_info = AccountInfo::new(ten_eth, 0, Bytecode::default());  
    fork_db.insert_account_info(account.into(), user_acc_info);  
  
    // Deploy Simulator contract  
    let simulator_address = H160::from_str("0xF2d01Ee818509a9540d8324a5bA52329af27D19E").unwrap();  
    let simulator_acc_info = AccountInfo::new(  
        rU256::ZERO,  
        0,  
        Bytecode::new_raw((*SIMULATOR_CODE.0).into()),  
    );  
    fork_db.insert_account_info(simulator_address.into(), simulator_acc_info);  
  
    let balance_slot = keccak256(&abi::encode(&[  
        abi::Token::Address(simulator_address.into()),  
        abi::Token::Uint(U256::from(input_balance_slot)),  
    ]));  
    fork_db.insert_account_storage(input_token.into(), balance_slot.into(), ten_eth)?;  
  
    // run v2SimulateSwap  
    let amount_in = U256::from(1)  
        .checked_mul(U256::from(10).pow(U256::from(18)))  
        .unwrap();  
    let simulator_abi = BaseContract::from(  
        parse_abi(&[  
            "function v2SimulateSwap(uint256,address,address,address) external returns (uint256, uint256)",  
        ])?  
    );  
    let calldata = simulator_abi.encode(  
        "v2SimulateSwap",  
        (amount_in, target_pair, input_token, output_token),  
    )?;  
  
    let gas_price = rU256::from(100)  
        .checked_mul(rU256::from(10).pow(rU256::from(9)))  
        .unwrap();  
    let v2_simulate_swap_tx = TxEnv {  
        caller: account.into(),  
        gas_limit: 5000000,  
        gas_price: gas_price,  
        gas_priority_fee: None,  
        transact_to: TransactTo::Call(simulator_address.into()),  
        value: rU256::ZERO,  
        data: calldata.0,  
        chain_id: None,  
        nonce: None,  
        access_list: Default::default(),  
    };  
    evm.env.tx = v2_simulate_swap_tx;  
  
    let result = match evm.transact_commit() {  
        Ok(result) => result,  
        Err(e) => return Err(anyhow!("EVM call failed: {:?}", e)),  
    };  
    let result = get_tx_result(result)?;  
    let out: (U256, U256) = simulator_abi.decode_output("v2SimulateSwap", result.output)?;  
    info!("Amount out: {:?}", out);  
  
    Ok(out)  
}

Very straightforward.

Update **(src/lib.rs)**:

pub mod revm_examples;  
pub mod constants;  
pub mod utils;  
pub mod trace;  
pub mod tokens;  
pub mod foundry_examples;

And add the below to our **(src/main.rs)**:

match foundry_v2_simulate_swap(  
    provider.clone(),  
    user,  
    weth_usdt_pair,  
    weth,  
    usdt,  
    weth_balance_slot,  
)  
.await  
{  
    Ok(_) => {}  
    Err(e) => info!("v2SimulateSwap foundry evm failed: {e:?}"),  
}

Output:

> **[18:47:53][INFO] Amount out: (1586529481, 1586529481)**

We see that we get the same output as our **revm** version.

## Simulating single transactions? Use eth_call!

Did you know that we could have done everything we did above without the help of a new library, and instead work our way up with **ethers-rs** alone?

We can use something called the **CallBuilder** to send **eth_call** requests to our RPC endpoint, which is documented here:

[

## Ethers.rs: The Ethereum Library for Rust

### A book on all things ethers-rs

www.gakonst.com







](https://www.gakonst.com/ethers-rs/providers/advanced_usage.html?source=post_page-----e01b5b0421e4---------------------------------------)

You don’t get the full picture from this documentation alone, but searching through some sample code in the **ethers-rs** Github repo will help you achieve this goal.

Create a new file called **(src/eth_call_examples.rs)**:

use anyhow::{anyhow, Result};  
use ethers::{  
    abi::{self, parse_abi},  
    core::utils::keccak256,  
    prelude::*,  
    providers::{call_raw::RawCall, Provider, Ws},  
    types::{spoof, TransactionRequest, H160, U256},  
};  
use log::info;  
use std::{str::FromStr, sync::Arc};  
  
use crate::constants::SIMULATOR_CODE;  
  
pub async fn eth_call_v2_simulate_swap(  
    provider: Arc<Provider<Ws>>,  
    account: H160,  
    target_pair: H160,  
    input_token: H160,  
    output_token: H160,  
    input_balance_slot: i32,  
) -> Result<(U256, U256)> {  
    // Shows how you can spoof multiple storage slots  
    // but also shows that you can only test one transaction at a time  
    let block = provider  
        .get_block(BlockNumber::Latest)  
        .await?  
        .ok_or(anyhow!("failed to retrieve block"))?;  
  
    let ten_eth = U256::from(10)  
        .checked_mul(U256::from(10).pow(U256::from(18)))  
        .unwrap();  
  
    // Spoof user balance with 10 ETH (for gas fees)  
    let mut state = spoof::state();  
    state.account(account).balance(ten_eth).nonce(0.into());  
  
    // Create Simulator contract with bytecode injection  
    let simulator_address = H160::from_str("0xF2d01Ee818509a9540d8324a5bA52329af27D19E").unwrap();  
    state  
        .account(simulator_address)  
        .code((*SIMULATOR_CODE).clone());  
  
    // Spoof simulator input token balance  
    let input_balance_slot = keccak256(&abi::encode(&[  
        abi::Token::Address(simulator_address),  
        abi::Token::Uint(U256::from(input_balance_slot)),  
    ]));  
    state.account(input_token).store(  
        input_balance_slot.into(),  
        H256::from_low_u64_be(ten_eth.as_u64()),  
    );  
  
    let one_eth = ten_eth.checked_div(U256::from(10)).unwrap();  
    let simulator_abi = BaseContract::from(  
        parse_abi(&[  
            "function v2SimulateSwap(uint256,address,address,address) external returns (uint256, uint256)",  
        ])?  
    );  
    let calldata = simulator_abi.encode(  
        "v2SimulateSwap",  
        (one_eth, target_pair, input_token, output_token),  
    )?;  
  
    let gas_price = U256::from(100)  
        .checked_mul(U256::from(10).pow(U256::from(9)))  
        .unwrap();  
    let tx = TransactionRequest::default()  
        .from(account)  
        .to(simulator_address)  
        .value(U256::zero())  
        .data(calldata.0)  
        .nonce(U256::zero())  
        .gas(5000000)  
        .gas_price(gas_price)  
        .chain_id(1)  
        .into();  
    let result = provider  
        .call_raw(&tx)  
        .state(&state)  
        .block(block.number.unwrap().into())  
        .await?;  
    let out: (U256, U256) = simulator_abi.decode_output("v2SimulateSwap", result)?;  
    info!("v2SimulateSwap eth_call result: {:?}", out);  
  
    Ok(out)  
}

Ahh the beauty of this code. It’s such a simple way to spoof data and make simulations on contracts that you didn’t deploy to the network yet. All you need is the contract’s bytecode.

Let’s try running this code after we update our **(src/lib.rs)**:

pub mod revm_examples;  
pub mod constants;  
pub mod utils;  
pub mod trace;  
pub mod tokens;  
pub mod foundry_examples;  
pub mod eth_call_examples;

And add the following in **(src/main.rs)**:

match eth_call_v2_simulate_swap(  
    provider.clone(),  
    user,  
    weth_usdt_pair,  
    weth,  
    usdt,  
    weth_balance_slot,  
)  
.await  
{  
    Ok(_) => {}  
    Err(e) => info!("v2SimulateSwap eth_call failed: {e:?}"),  
}

Output:

> **[18:57:03][INFO] v2SimulateSwap eth_call result: (1588524014, 1588524014)**

As expected, we see the exact same results.

## **Show me some benchmarks. How fast is REVM?**

After implementing three different methods for conducting EVM simulations, we may be curious about their respective performance

To assess this, I conducted a straightforward benchmark to compare the time taken by each method to complete a complex simulation similar to the one we conducted today.

_(These simulations can be done in so many different ways, so please take the results here with a grain of salt!)_

You can refer to the following link to review the details of how these benchmarks were carried out:

[

## revm-is-all-you-need/src/benchmarks.rs at main · solidquant/revm-is-all-you-need

### EVM simulation tutorials and samples (revm / foundry-evm / eth_call) - revm-is-all-you-need/src/benchmarks.rs at main ·…

github.com



](https://github.com/solidquant/revm-is-all-you-need/blob/main/src/benchmarks.rs?source=post_page-----e01b5b0421e4---------------------------------------)

Enter our first player:

**🏎 1. revm:**

Press enter or click to view image in full size

![](https://miro.medium.com/v2/resize:fit:1400/1*wZ4sIZaiw5sHBPZTG_UKug.png)

**🏎 2. foundry-evm:**

Press enter or click to view image in full size

![](https://miro.medium.com/v2/resize:fit:1400/1*POw0DeaOKiaj3T6cQVRgsA.png)

**🏎 3. eth_call:**

Press enter or click to view image in full size

![](https://miro.medium.com/v2/resize:fit:1400/1*36i5GIHjNVZ1xY29YG4z8Q.png)

🚀⚡️That’s quite unexpected isn’t it?

> **_revm_** _took:_ **_0.06 seconds_** _on average_
> 
> **_foundry-evm_** _took:_ **_0.12 seconds_** _on average_
> 
> _and_ **_eth_call_** _took:_ **_0.005 seconds_** _on average_

using a personal Geth node.

I’d also like to share the results of the same benchmarks using an **Alchemy node** for fun:

> **_revm_** _took:_ **_1.02 seconds_** _on average_
> 
> **_foundry-evm_** _took:_ **_4.7_** **_seconds_** _on average_
> 
> _and_ **_eth_call_** _took:_ **_0.25 seconds_** _on average_

As anticipated, the simulations proved to be excessively time-consuming when using a public node service.

We can draw the preliminary conclusion that **eth_call** is the most efficient approach for EVM simulations, based on the observed results.

**However**, it’s essential to have a comprehensive understanding of the strengths and weaknesses of each method to selectively apply them to suit our specific requirements.

The reason why **eth_call** was the fastest in our case is because we had to make multiple calls to the node endpoint trying to retrieve storage values.

Therefore, when dealing with the simulation of a single transaction, particularly one that involves accessing multiple storage values within a complex transaction, **eth_call** appears to be the preferred method.

Conversely, in scenarios where real-time updates to our EVM instance can be achieved through subscription to event streams, or when dealing with bundled transactions, utilizing **revm** or **foundry-evm** is a more practical choice.

**These methods each have their unique use cases, and it is imperative to invest the time in comprehending the technology before forming definitive conclusions.**

## What to expect next

We covered a a lot of ground today, and **I want to congratulate you on reaching the end**. (I’m not entirely certain if anyone is still reading this article at this point, but here we are! 🤣) I genuinely hope that this journey has been fulfilling for those who have stuck around.

Moving forward, in the upcoming blog posts, we will explore how EVM simulation can be leveraged to assist in the development of MEV bots. The code for this endeavor has already been made available on GitHub, so anyone interested can check out the repository ahead of my forthcoming articles:

[

## GitHub - solidquant/evm-simulation: EVM Simulation using REVM + Foundry

### EVM Simulation using REVM + Foundry. Contribute to solidquant/evm-simulation development by creating an account on…

github.com



](https://github.com/solidquant/evm-simulation?source=post_page-----e01b5b0421e4---------------------------------------)

We’ll look at:

1. 🍯 **How to filter out honeypot tokens**
2. 🌯 **How to simulate sandwich bundles**
3. 📈 **How to simulate arbitrage paths**

Moreover, I’m going to dive into **pyrevm** and start showing you samples on how all this can be done using Python.

**If you have any feedback, please feel free to leave a comment below or come join us on Discord 👾 and send me a direct message anytime. We’re gradually expanding our community to collaborate and develop MEV tools that can help people. 🤝🏻**

I’m very interested in subjects related to MEV/quant related stuff, so I’m always here to listen to your feedbacks. Once again, thank you very much for taking the time to read my blog posts! It truly means the whole world to me! **Like always, let’s experience awesome together 😎🙏**

